default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: root
  password: password
  host: 127.0.0.1  # localhost ではなく 127.0.0.1 を推奨
  port: 3306

development:
  <<: *default
  database: ptera_cup_development